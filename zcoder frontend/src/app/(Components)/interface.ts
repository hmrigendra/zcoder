interface commentData {
    questionId: string;
  email: string;
  comment: string;
}

interface problemData {
    questionId: string;
  email: string;
  questionHeader: string;
  question: string;
  answer: string;
}
